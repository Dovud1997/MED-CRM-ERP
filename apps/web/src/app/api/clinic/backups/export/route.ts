import {NextRequest,NextResponse} from 'next/server';import{clinicRequest}from'@/lib/server-api';
export async function POST(request:NextRequest){const{upstream,rotated}=await clinicRequest(request,'/backups/export',{method:'POST',body:await request.text()});const response=new NextResponse(await upstream.arrayBuffer(),{status:upstream.status,headers:{'content-type':upstream.headers.get('content-type')??'application/octet-stream','content-disposition':upstream.headers.get('content-disposition')??'attachment; filename="backup.ovbk"'}});if(rotated){const secure=process.env.COOKIE_SECURE==='true';response.cookies.set('obk_access',rotated.accessToken,{httpOnly:true,sameSite:'strict',secure,path:'/',maxAge:900});response.cookies.set('obk_refresh',rotated.refreshToken,{httpOnly:true,sameSite:'strict',secure,path:'/api/session',maxAge:2592000});}return response;}
